export const ELECTRUM_METHODS = {
  SERVER_VERSION: 'server.version',
  HEADERS_SUBSCRIBE: 'blockchain.headers.subscribe',
  SERVER_PEERS_SUBSCRIBE: 'server.peers.subscribe',
};

export const HEALTH_CHECK_TIMEOUT_IN_MS = 8000;

export const MAX_SERVERS_TO_RACE = 15;
